package main

import (
	t "Golang_practice/tradingbase/trading"
)

func main() {

	// t.SearchStockDetails("WAAREEENER")
	// t.GetStockDetails(18154) //if no options stock, "toi" key value will not be there
	// t.PlaceOrder("NIFTY13JAN26P25700", "1", 10.0, 8, 40, t.GetTrailingStopLoss(40))

	// t.StockOptionsFetchAndValidation()
	// fmt.Println(t.GetCurrentTimestamp())

	// fmt.Println(os.Getwd())
	// file, err := os.Open("d:/GolangPractice/go/src/GolangWay/tradingbase/NiftyLotSize.csv")
	// fmt.Println(file, err)
	// defer file.Close()

	// fmt.Println(strings.Replace("ssssathish", "s", "$", 3))
	// fmt.Println(strings.TrimRight("ssssathish", "s"))

	t.Dummy()

}
